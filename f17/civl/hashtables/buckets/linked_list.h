struct Node{
    int val;
    struct Node* previous;
    struct Node* next;
};

// add a node
int n_add(struct Node n){

};

// remove a node
int n_remove(struct Node n);

// check if node n is in the list
int n_contains(struct Node n);
